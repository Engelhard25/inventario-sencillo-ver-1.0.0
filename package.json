{
  "name": "inventario",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db-inventory.json --port 3001",
    "server2": "json-server server/rate.json --port 3002"
  },
  "devDependencies": {
    "vite": "^4.3.2"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
